FROM nvcr.io/nvidia/base/ubuntu:24.04
RUN apt-get update -qq && \
    apt-get install -y --no-install-recommends \
      curl ca-certificates dnsutils python3 iproute2 netcat-openbsd && \
    apt-get clean && rm -rf /var/lib/apt/lists/*
